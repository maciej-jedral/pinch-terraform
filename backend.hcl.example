# Copy to backend.hcl (gitignored). ./tf init picks it up automatically.
# The bucket name is printed by the bootstrap module (bootstrap/ -> output state_bucket_name).
bucket = "pinch-tfstate-<your-aws-account-id>"
